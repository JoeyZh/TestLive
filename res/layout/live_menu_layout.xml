<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    >

    <ListView
        android:id="@+id/top_menu_list"
        android:layout_width="@dimen/top_menu_list_width"
        android:layout_height="match_parent"
        android:divider="@android:color/transparent"
        android:drawSelectorOnTop="false"
        android:listSelector="@color/item_selector_color"
        android:scrollbars="none"
         >
    </ListView>

    <com.taixin.boxassistant.live.widget.ListViewCompat
        android:id="@+id/channel_list"
        android:layout_width="@dimen/channel_list_width"
        android:layout_height="match_parent"
        android:divider="@android:color/transparent"
        android:drawSelectorOnTop="false"
        android:listSelector="@color/item_selector_color"
        android:scrollbars="none" />

    <ListView
        android:id="@+id/epg_list"
        android:layout_width="@dimen/epg_list_width"
        android:layout_height="match_parent"
        android:divider="@android:color/transparent"
        android:drawSelectorOnTop="false"
        android:listSelector="@color/item_selector_color"
        android:visibility="invisible"
        android:scrollbars="none"
         >
    </ListView>

</merge>