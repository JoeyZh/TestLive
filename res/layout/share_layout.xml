<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:gravity="center" >

    <RelativeLayout
        android:id="@+id/sharelayout_titlebar_root"
        android:layout_width="fill_parent"
        android:layout_height="@dimen/status_layout_height"
        >

        <TextView
            android:id="@+id/sharelayout_title_textView"
            android:layout_width="wrap_content"
            android:layout_height="fill_parent"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:text="@string/share_to_friend"
            android:textSize="@dimen/top_menu_text_size"
            android:textColor="@android:color/black" />
 	</RelativeLayout>
 		
        <ImageView
            android:id="@+id/share_image_view"
            android:layout_below="@+id/sharelayout_titlebar_root"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            >
        </ImageView>
   

    <HorizontalScrollView
        android:id="@+id/share_scroll_layout"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/share_image_view"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp" >

        <LinearLayout
            android:id="@+id/share_list_root"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/white"
            android:orientation="horizontal"
            android:gravity="center" >
        </LinearLayout>
    </HorizontalScrollView>

</RelativeLayout>